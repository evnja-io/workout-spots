# Production image CDN (Cloudflare Worker reverse-proxy on img.evnja.gg).
# Committed so prod/preview builds reliably inline it via Vite (import.meta.env),
# independent of Vercel dashboard env config. Safe: public hostname, no secret.
VITE_IMAGE_CDN_URL=https://img.evnja.gg
