# Workout Spots Discover — SDD progress

Plan: docs/superpowers/plans/2026-06-24-workout-spots-discover.md
Mode: mocks-first (no live keys yet); Tailwind v4 styling
Branch: feature/workout-spots-discover
Branch base for final review: e2f1f60

## Tasks

- [x] Task 1: Project scaffold (TanStack Start + TS strict + Vite + Tailwind v4) — commits f85e370, a3fa063. Reviewed: APPROVE (2 Minor; `directories` leftover fixed). typecheck ✅, dev server serves /spots ✅. Deviations: `"type":"module"` (ESM), `getRouter` export (Start server-core requirement), no index.html (SSR uses \_\_root.tsx).
- [x] Task 2: Lint + Prettier + no-any — commit 6decd43. Verified: no-any rule fires on temp `any`; `npm run lint` exits 0 clean. Deviations: `allowDefaultProject:['eslint.config.js']`, `only-throw-error:'off'` (TanStack `throw redirect()`; broad — revisit later), prettier normalized 3 scaffold files.
- [x] Task 3: Test harness (Vitest + RTL + MSW + Mapbox mock) — commit ee58d92. No deviations. Independently verified: `npm test` 1 passed, `npm run lint` clean, `npm run typecheck` clean. MSW 2.14.6 (v2 API).
- [x] Task 16: MapView (real Mapbox) + style switch + controls — commit 32957c0. SSR-safe (dynamic `import('mapbox-gl')` in client effect, no top-level import), token-notice when no token (mocks-first), markers/style/init effects, MapStyleSwitch, mapbox-gl.css imported. 60 tests pass; SSR /spots 200 (token-notice). No `any`.
- [x] Task 15: Sidebar + Filters + SpotCard (virtualized) — commit dd085b8. Presentational SpotCard/Filters + smart Sidebar (useNavigate, parsed search, useSuspenseQuery + applyFilters, debounced search, empty state). **Mocks-first guard:** `isSupabaseConfigured()` added; spots+equipments+disciplines queryFns return [] when unconfigured → SSR empty Discover. Virtualize only when >30 items (jsdom-friendly). 58 tests pass; SSR /spots 200 + sidebar rendered. Deviations: spot-click uses `window.location.href` until Task 17 adds `/spots/$spotId` route (**Task 17 must swap to typed `navigate`/`Link`**). No `any`.
- [x] Task 14: App shell — Rail + Discover layout + QueryClientProvider + validateSearch — commit 2e83668. Rail (Map/List/Saved/Settings, no Community), .app grid in route.tsx, QueryClientProvider in __root + client theme/accent applier. 50 tests pass; SSR /spots 200 + ?sort=bogus 200 (rail SSR'd). Deviations: (1) added `spotRouteSearchSchema` (all-optional, no defaults) for validateSearch to avoid 307 default-param redirect — components use `spotSearchSchema.parse(useSearch())` for defaults; (2) Saved is `<a href="/saved">` until Task 18 adds the route (swap to `<Link>` then); (3) ThemeApplier subcomponent. No `any`.
- [x] Task 13: Auth — magic-link, SessionProvider, useAuthGate, /auth/callback — commit e94e9e3. SSR-safe provider (loading→getSession in effect, onAuthStateChange cleanup), SignInModal mounted in provider (app-wide gate), exchangeCodeForSession callback (context7-verified). auth.* i18n EN+FR. 47 tests pass; lint+typecheck+build clean; **SSR /spots HTTP 200 runtime-verified**. No `any`.
- [x] Task 12: Spot read queries + row mappers — commit 580451b. Pure mapSpotRow/mapSpotDetailRow (10 tests), spotsQueryOptions ['spots'] + spotDetailQueryOptions ['spot',id] via browser client. **getSpotDetailFn (createServerFn) DEFERRED to T17/18** (route wiring + runtime verify; avoids untested server-fn). viewerLiked=false/viewerRating=null until then. 43 tests pass, lint+typecheck clean, no `any`.
- [x] Task 11: Taxonomy queries (equipments + disciplines) — commit 9445aab. equipmentsQueryOptions/disciplinesQueryOptions (queryOptions, keys ['equipments']/['disciplines']) + resolveLabel(localeKey,fallback,t). Row typing via `Pick<Database...>`, no `any`. 33 tests pass, lint+typecheck clean. Establishes the Supabase queryOptions pattern.
- [x] Task 10: UI primitives (Button, Chip, Switch, Stars, Modal) + cx helper — commit 00f74ed. Prototype classes + a11y (role=switch/dialog, aria-pressed, Stars=5 buttons, Esc-close, focus trap, SSR-safe portal). No `any`. 31 tests pass, lint+typecheck clean.
- [x] Task 9: Icon component (port) — commit ec82845. 26-icon typed `Icon` (IconName union), verbatim SVG paths from prototype, `aria-hidden`. No deviations. 11 tests pass, lint+typecheck clean.
- [x] Task 8: Spot domain types + filter/sort + search schema — commit c7f6e52. domain.ts (SpotListItem/SpotDetail/Equipment/Discipline/SpotImage/SpotComment), filters.ts (Zod spotSearchSchema w/ catch+default, applyFilters w/ sort folded in). No deviations. 10 tests pass, lint+typecheck clean. zod installed.
- [x] Task 7: Mapbox geocoding client — commit f39e5c3. forwardGeocode/reverseGeocode/mapStyleUrl, verbatim from plan. Deviation: `import.meta.env.VITE_MAPBOX_TOKEN as string | undefined` (matches browser.ts pattern; avoids no-unsafe-assignment). 6 tests pass, lint+typecheck clean, no `any`.
- [x] Task 6: i18n (EN + FR) — commits 06fc43e + fix 85bd3db. react-i18next, per-request fresh instance, locale via `lang` cookie through `createIsomorphicFn` (client: document.cookie; server: locale.server.ts → getCookies). **Critical bug caught in review:** implementer used `require()` in the `.server()` branch → ESM SSR crash (HTTP 500); build passed but runtime failed. Fixed to static import; runtime-verified HTTP 200 + `lang=en/fr` by cookie. Lesson saved → memory [[ssr-runtime-verification]]; future SSR briefs require runtime curl, not just build.
- [x] Task 5: Supabase types + client factories — commit 956c853. Hand-written `Database` (10 tables incl. `location_ratings` w/ TODO(db-access); `Json` type for jsonb; zero any). browser.ts singleton + server.ts cookie wiring. Deviation: plan's cookie API snippet was stale — adapted via context7 to `getCookies`/`setCookie`/`setResponseHeader`; `setAll(toSet, headers)` 2-arg form is CORRECT for installed @supabase/ssr (verified against its types — prevents CDN caching of auth cookies). Verified: browser test pass, full test 4 passed, typecheck+lint clean. **Pending DB-access:** swap to `supabase gen types`; confirm location_ratings columns + average_rating trigger.
- [x] Task 4: Global styles + theme/accent helpers — commit 18a9251. **Deliberate deviation:** plan's Task 4 described minimal CSS port + utility-rebuilt components, but Tasks 10/14–21 say "port markup/class names". Resolved by porting FULL prototype CSS into `@layer components` + `@theme inline` tokens (controller authored global.css, 1254 lines). theme.ts verbatim from plan (4 accents × 2 themes). Verified: theme tests pass, `npm run build` exit 0 (CSS compiles, 25kB emitted), test/lint/typecheck clean. Later component tasks apply prototype class names.
